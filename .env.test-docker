# Copy to .env before: docker compose up -d --build
# cp .env.docker.example .env

# --- CV workload ---
CV_API_PORT=8000
CV_UI_PORT=8501
# Used in MLAir manifest image_uri — must be reachable FROM ml-air-api container
CV_API_BASE_URL=http://cv-api:8000
CV_ARTIFACT_ROOT=artifacts
CV_PERSIST_JOBS=1
CV_MLAIR_AUTO_INGEST=1
CV_MLAIR_AUTO_MATERIALIZE=0
CV_MLAIR_DATASET_NAME=cv-traffic-frames

# MLAir client (cv-api → mlair service on compose network)
CV_MLAIR_API_URL=http://mlair:8080
CV_MLAIR_TOKEN=admin-token
CV_MLAIR_TENANT=default
CV_MLAIR_PROJECT=default_project

# --- MLAir quickstart (included compose) ---
ML_AIR_API_PORT=8080
ML_AIR_FRONTEND_PORT=38080
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
ML_AIR_TRACKING_TOKEN=admin-token
ML_AIR_JWT_HS256_SECRET=mlair-dev-jwt-secret-32bytes-minimum-key
POSTGRES_USER=mlair
POSTGRES_PASSWORD=mlair
POSTGRES_DB=mlair
