[server]
# Match CV_DATASET_ZIP_MAX_MB (in MB). Streamlit default is 200MB.
maxUploadSize = 2048
# AWS ALB path prefix /cv (see docker-compose.aws.yml --server.baseUrlPath=/cv).
baseUrlPath = "/cv"
enableCORS = false
enableXsrfProtection = false
headless = true

[browser]
gatherUsageStats = false

