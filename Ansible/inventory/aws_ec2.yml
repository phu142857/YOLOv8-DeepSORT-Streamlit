# Auto-generated by deploy.sh — 2026-05-23T21:27:57+07:00
# EC2 is in a private subnet — SSM Session Manager (no SSH/bastion).
# Requires: ansible-galaxy collection install community.aws
all:
  children:
    cv_mlair:
      hosts:
        app:
          ansible_host: 
          ansible_user: ec2-user
          ansible_connection: community.aws.aws_ssm
          ansible_aws_ssm_instance_id: 
          ansible_aws_ssm_region: ap-southeast-1
